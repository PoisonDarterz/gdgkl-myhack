---
phase: 04-event-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/TimelineSection.tsx
  - src/components/sections/PartnersSection.tsx
autonomous: true
requirements:
  - SECT-05
  - SECT-06

must_haves:
  truths:
    - "Timeline section shows 5-6 event entries each with a date/time left column and a bordered fixed-height event card on the right"
    - "Each timeline card shows the event name, a truncated description, and a type badge with a filled background color unique to the event type"
    - "Partners section shows Google displayed with logo and DIAMOND SPONSOR label, oversized and centered"
    - "Partners section shows Gold, Silver, and Bronze tier rows below Diamond, each visually smaller than the tier above, with placeholder logo boxes"
  artifacts:
    - path: "src/components/sections/TimelineSection.tsx"
      provides: "Timeline section with date columns, fixed-height event cards, and type badges"
      min_lines: 50
    - path: "src/components/sections/PartnersSection.tsx"
      provides: "Partners section with tiered sponsor display"
      min_lines: 50
  key_links:
    - from: "src/components/sections/TimelineSection.tsx"
      to: "typeBadgeStyles map"
      via: "event.type key lookup for badge className"
      pattern: "typeBadgeStyles\\[.*\\.type\\]"
    - from: "src/components/sections/PartnersSection.tsx"
      to: "four tier sections"
      via: "Diamond, Gold, Silver, Bronze rendered with distinct sizing"
      pattern: "DIAMOND|GOLD|SILVER|BRONZE"
---

<objective>
Implement TimelineSection and PartnersSection, replacing their placeholder stubs with fully styled sections that match the Figma design.

Purpose: Delivers the event schedule and sponsors display — two of the three Phase 4 sections required before the phase can ship.
Output: Two implemented server-component section files.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sections/BenefitsSection.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TimelineSection</name>
  <files>src/components/sections/TimelineSection.tsx</files>
  <action>
Replace the placeholder stub entirely. Keep it a server component — no "use client" directive.

**Color tokens available (from globals.css):**
- `brand-text` = #282828, `brand-muted` = #5C5C5C, `brand-bg` = #F5F5F5
- `font-mono` = Courier Prime (brand monospace)

**Header row pattern** — match BenefitsSection exactly:
```tsx
<div className="flex items-center gap-3 mb-6">
  <span className="font-mono text-xs font-bold border border-brand-text px-2 py-0.5 tracking-wider">
    [ TIMELINE ]
  </span>
  <span className="font-mono text-xs text-brand-muted tracking-widest">{`>>>`}</span>
  <div className="flex-1 border-t border-dashed border-brand-muted/60" />
</div>
```

**Data array** — hardcode at top of file:
```ts
const events = [
  { date: "DAY 1", time: "09:00", name: "OPENING KEYNOTE", description: "Welcome ceremony and event overview with featured speakers from Google and GDG KL.", type: "keynote" },
  { date: "DAY 1", time: "10:30", name: "GEMINI API WORKSHOP", description: "Hands-on session building with the Gemini API — from first call to a working prototype deployed on Firebase.", type: "workshop" },
  { date: "DAY 1", time: "14:00", name: "VERTEX AI DEEP DIVE", description: "Technical deep dive into Vertex AI capabilities and deployment patterns for production-ready AI applications.", type: "talk" },
  { date: "DAY 2", time: "09:00", name: "HACKATHON KICKOFF", description: "Teams lock in their ideas and begin building AI-powered solutions using Google's developer toolchain.", type: "hackathon" },
  { date: "DAY 2", time: "15:00", name: "PROJECT SHOWCASE", description: "Teams present completed projects to a panel of judges from Google and the developer community.", type: "showcase" },
  { date: "DAY 2", time: "17:30", name: "AWARDS + CLOSING", description: "Prize announcements, community recognitions, and closing remarks from GDG KL organisers.", type: "keynote" },
] as const;
```

**Type badge color map** — hardcode at top of file:
```ts
const typeBadgeStyles: Record<string, string> = {
  workshop:  "bg-[#4CAF50] text-white",
  talk:      "bg-[#2196F3] text-white",
  keynote:   "bg-[#FFD600] text-[#282828]",
  hackathon: "bg-[#FF9800] text-white",
  showcase:  "bg-[#9C27B0] text-white",
};
```

**Each event entry layout** — flex row:
- Left column (w-24 shrink-0): date (font-mono text-xs font-bold text-brand-text uppercase) stacked above time (font-mono text-xs text-brand-muted)
- Right card (flex-1 border border-brand-text h-28 overflow-hidden relative p-3 flex flex-col justify-between):
  - Top: event name (font-mono text-sm font-bold text-brand-text uppercase tracking-wider)
  - Middle: description (font-mono text-xs text-brand-muted leading-relaxed line-clamp-2 overflow-hidden)
  - Bottom: type badge (self-end, font-mono text-[9px] uppercase tracking-widest px-2 py-0.5 using typeBadgeStyles[event.type])

**Entries list wrapper:** `<div className="flex flex-col gap-3">` — no connecting lines or dots between entries (per user decision).

**Section wrapper:** `<section className="px-6 lg:px-8 py-6">`

**Full structure:**
```tsx
export function TimelineSection() {
  return (
    <section className="px-6 lg:px-8 py-6">
      {/* header row */}
      <div className="flex items-center gap-3 mb-6">
        <span className="font-mono text-xs font-bold border border-brand-text px-2 py-0.5 tracking-wider">
          [ TIMELINE ]
        </span>
        <span className="font-mono text-xs text-brand-muted tracking-widest">{`>>>`}</span>
        <div className="flex-1 border-t border-dashed border-brand-muted/60" />
      </div>
      {/* event entries */}
      <div className="flex flex-col gap-3">
        {events.map((event) => (
          <div key={event.name} className="flex items-stretch gap-4">
            {/* date/time column */}
            <div className="w-24 shrink-0 flex flex-col justify-center gap-0.5">
              <span className="font-mono text-xs font-bold text-brand-text uppercase">{event.date}</span>
              <span className="font-mono text-xs text-brand-muted">{event.time}</span>
            </div>
            {/* event card */}
            <div className="flex-1 border border-brand-text h-28 overflow-hidden relative p-3 flex flex-col justify-between">
              <span className="font-mono text-sm font-bold text-brand-text uppercase tracking-wide">
                {event.name}
              </span>
              <p className="font-mono text-xs text-brand-muted leading-relaxed line-clamp-2">
                {event.description}
              </p>
              <div className="flex justify-end">
                <span className={`font-mono text-[9px] uppercase tracking-widest px-2 py-0.5 ${typeBadgeStyles[event.type]}`}>
                  {event.type}
                </span>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>
  );
}
```
  </action>
  <verify>Run `npx tsc --noEmit` from project root — exits 0 with no TypeScript errors. The file exports `TimelineSection` as a named export and does not use "use client".</verify>
  <done>TimelineSection renders 6 event entries with date/time left columns, fixed-height bordered cards showing name + description + type badge, no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PartnersSection</name>
  <files>src/components/sections/PartnersSection.tsx</files>
  <action>
Replace the placeholder stub entirely. Keep it a server component — no "use client" directive.

**Header row pattern** — same as BenefitsSection/TimelineSection:
```tsx
<div className="flex items-center gap-3 mb-6">
  <span className="font-mono text-xs font-bold border border-brand-text px-2 py-0.5 tracking-wider">
    [ PARTNERS ]
  </span>
  <span className="font-mono text-xs text-brand-muted tracking-widest">{`>>>`}</span>
  <div className="flex-1 border-t border-dashed border-brand-muted/60" />
</div>
```

**Diamond tier** — centered, oversized, prominent (per user decision):
- Outer wrapper: `<div className="flex flex-col items-center border border-brand-text p-8 mb-8 mx-auto max-w-xs">`
- Google logo as colored styled text (no new image imports; inline approach): `<p className="font-sans text-5xl font-black tracking-tight mb-2"><span className="text-[#4285F4]">G</span><span className="text-[#EA4335]">o</span><span className="text-[#FBBC05]">o</span><span className="text-[#34A853]">g</span><span className="text-[#EA4335]">l</span><span className="text-[#4285F4]">e</span></p>`
- Below: `<span className="font-mono text-[10px] uppercase tracking-[0.3em] text-brand-muted">Diamond Sponsor</span>`

**Tier rows** — centered layout with tier label left and placeholder boxes right:

Gold (2 boxes, w-32 h-16):
```tsx
<div className="flex items-center justify-center gap-6 mb-6">
  <span className="font-mono text-xs uppercase tracking-widest text-brand-muted w-16 text-right shrink-0">GOLD</span>
  <div className="flex gap-4">
    {[1, 2].map(n => (
      <div key={n} className="w-32 h-16 border border-dashed border-brand-muted/60 flex items-center justify-center">
        <span className="font-mono text-[9px] uppercase tracking-wider text-brand-muted/60">SPONSOR</span>
      </div>
    ))}
  </div>
</div>
```

Silver (3 boxes, w-24 h-12):
```tsx
<div className="flex items-center justify-center gap-6 mb-4">
  <span className="font-mono text-xs uppercase tracking-widest text-brand-muted w-16 text-right shrink-0">SILVER</span>
  <div className="flex gap-3">
    {[1, 2, 3].map(n => (
      <div key={n} className="w-24 h-12 border border-dashed border-brand-muted/60 flex items-center justify-center">
        <span className="font-mono text-[9px] uppercase tracking-wider text-brand-muted/60">SPONSOR</span>
      </div>
    ))}
  </div>
</div>
```

Bronze (4 boxes, w-20 h-10):
```tsx
<div className="flex items-center justify-center gap-6 mb-4">
  <span className="font-mono text-xs uppercase tracking-widest text-brand-muted w-16 text-right shrink-0">BRONZE</span>
  <div className="flex gap-2">
    {[1, 2, 3, 4].map(n => (
      <div key={n} className="w-20 h-10 border border-dashed border-brand-muted/60 flex items-center justify-center">
        <span className="font-mono text-[9px] uppercase tracking-wider text-brand-muted/60">SPONSOR</span>
      </div>
    ))}
  </div>
</div>
```

**Section wrapper:** `<section className="px-6 lg:px-8 py-6">`

The complete component wraps everything in the section wrapper with the header row at top, then the Diamond tier centered block, then the Gold/Silver/Bronze rows.
  </action>
  <verify>Run `npx tsc --noEmit` from project root — exits 0. The file exports `PartnersSection` as a named export. Does not use "use client". Google styled text is visible and tier labels DIAMOND/GOLD/SILVER/BRONZE are present in the markup.</verify>
  <done>PartnersSection renders Google with colored styled text as Diamond Sponsor (large, centered, bordered), followed by Gold (2 boxes), Silver (3 boxes), Bronze (4 boxes) tier rows with visually decreasing box sizes. No TypeScript errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` exits 0 from project root
2. Both files contain named exports matching their import names in `src/components/sections/index.ts`
3. No "use client" directive in either file (server components)
4. TimelineSection: 6 event entries each with date/time left + card right + type badge
5. PartnersSection: Diamond Google block + 3 subordinate tier rows with placeholder boxes
</verification>

<success_criteria>
- TimelineSection.tsx fully implemented with 6 entries, fixed-height cards, type badge color map, no TypeScript errors
- PartnersSection.tsx fully implemented with Google Diamond block (oversized, bordered, centered) and Gold/Silver/Bronze tier rows (each tier visually smaller)
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-sections/04-01-SUMMARY.md`
</output>
