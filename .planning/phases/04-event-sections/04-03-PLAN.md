---
phase: 04-event-sections
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - SECT-05
  - SECT-06
  - SECT-07

must_haves:
  truths:
    - "npm run build exits 0 with no TypeScript or compilation errors"
    - "Timeline, Partners, and FAQ sections all render visibly in the browser with no blank or placeholder content"
    - "FAQ accordion opens and closes items smoothly when clicked"
    - "Multiple FAQ items can be open simultaneously"
  artifacts:
    - path: "src/components/sections/TimelineSection.tsx"
      provides: "Implemented — not a stub"
    - path: "src/components/sections/PartnersSection.tsx"
      provides: "Implemented — not a stub"
    - path: "src/components/sections/FAQSection.tsx"
      provides: "Implemented — not a stub"
  key_links:
    - from: "npm run build"
      to: "exit code 0"
      via: "Next.js build process"
      pattern: "Compiled successfully|Route.*\\(Static\\)"
---

<objective>
Run a production build check and then visually verify all three Phase 4 sections in the browser.

Purpose: Confirms the phase ships without TypeScript errors or build failures, and that the visual output matches the user's expectations before moving to Phase 5.
Output: No new files — this plan validates the work completed in Plans 01 and 02.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-event-sections/04-01-SUMMARY.md
@.planning/phases/04-event-sections/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build check</name>
  <files></files>
  <action>
Run the Next.js production build from the project root to verify the three implemented sections compile without errors:

```bash
npm run build
```

If the build fails with TypeScript errors in the section files:
- Read the error message and the offending file
- Fix the specific error (type mismatch, missing import, invalid JSX)
- Re-run `npm run build` until it exits 0

Common issues to watch for:
- `as const` on event array requires individual type assertions if `typeBadgeStyles` lookup fails — add a fallback: `typeBadgeStyles[event.type] ?? ""`
- `Set<number>` iteration in React JSX requires Array.from() — not applicable here since only `.has()` is used
- Missing `key` prop on mapped elements — ensure all `.map()` calls have a unique `key`
  </action>
  <verify>`npm run build` exits 0. Terminal output shows "Compiled successfully" or similar Next.js success message with no red error lines.</verify>
  <done>Production build passes with exit code 0. No TypeScript or compilation errors in any section file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all three sections</name>
  <what-built>
Three Phase 4 sections implemented in Plans 01 and 02:
- TimelineSection: 6 event entries with date/time left columns, fixed-height bordered cards, event type badges (green workshop, blue talk, yellow keynote, orange hackathon, purple showcase)
- PartnersSection: Google as Diamond Sponsor (colored styled text, bordered centered block), followed by Gold (2 boxes), Silver (3 boxes), Bronze (4 boxes) tier rows with placeholder boxes
- FAQSection: 6 accordion items inside a single bordered container, multi-open, smooth height animation, rotating chevron
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000 in the browser
2. Scroll down past HeroSection and BenefitsSection
3. **Timeline:** Verify you see 6 event rows — each with a DAY 1 / DAY 2 date label on the left and a fixed-height bordered card on the right. Confirm at least 3 distinct badge colors are visible (keynote=yellow, workshop=green, talk=blue)
4. **Partners:** Verify Google appears with colored letter styling and "DIAMOND SPONSOR" label in a bordered box. Confirm GOLD, SILVER, and BRONZE rows are visible below it, each with placeholder boxes that get progressively smaller
5. **FAQ:** Verify 6 questions are listed inside a bordered container. Click any question — the answer should expand smoothly. Click it again — it should collapse smoothly. Click two different questions — both should be open at the same time. Confirm the chevron points down when closed and rotates to point up when open
  </how-to-verify>
  <action>Run `npm run dev`, open http://localhost:3000 in the browser, scroll down past HeroSection and BenefitsSection, and verify all three sections using the steps in how-to-verify. This is a human-only verification step — no code changes required unless issues are found.</action>
  <resume-signal>Type "approved" if all three sections look correct, or describe specific issues to fix (e.g. "FAQ chevron not rotating", "Partners tiers not visible")</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when:
1. `npm run build` exits 0
2. All three sections render with real content (no placeholder text)
3. FAQ accordion is interactive and correct
4. User has typed "approved" for the visual checkpoint
</verification>

<success_criteria>
- npm run build exits 0 with no errors
- Timeline shows 6 event entries with date columns, fixed-height cards, and type badges
- Partners shows Google Diamond block + three subordinate tier rows
- FAQ shows 6 items with working multi-open accordion, smooth animation, and chevron rotation
- User visually approved all three sections
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-sections/04-03-SUMMARY.md`
</output>
