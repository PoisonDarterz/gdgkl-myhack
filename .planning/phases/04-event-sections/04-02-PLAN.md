---
phase: 04-event-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/FAQSection.tsx
autonomous: true
requirements:
  - SECT-07

must_haves:
  truths:
    - "FAQ section shows 6 Q&A items in a single bordered list container"
    - "Clicking a question header toggles the answer open or closed with a smooth height transition"
    - "Multiple FAQ items can be open at the same time — no auto-close behavior"
    - "A chevron icon rotates 180 degrees when the item is open and returns to 0 degrees when closed"
  artifacts:
    - path: "src/components/sections/FAQSection.tsx"
      provides: "Interactive FAQ accordion with open/close animation and multi-open state"
      exports: ["FAQSection"]
      min_lines: 60
  key_links:
    - from: "src/components/sections/FAQSection.tsx"
      to: "useState<Set<number>>"
      via: "openItems set tracks each item index independently — no auto-close"
      pattern: "useState.*Set"
    - from: "answer wrapper div"
      to: "max-h Tailwind transition"
      via: "conditional max-h-0 / max-h-48 class based on openItems.has(i)"
      pattern: "max-h-0|max-h-48"
    - from: "chevron svg"
      to: "rotate-180 class"
      via: "conditional class based on openItems.has(i)"
      pattern: "rotate-180"
---

<objective>
Implement FAQSection as an interactive accordion, replacing the placeholder stub with a fully functional component.

Purpose: Delivers the FAQ section — the interactive, client-side component of Phase 4. The accordion requires React state and animation; it is deliberately isolated in its own plan to keep context focused on the stateful implementation.
Output: FAQSection.tsx — a "use client" accordion with smooth CSS transitions, multi-open state, and chevron rotation.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/components/sections/WhatIsBAISection.tsx
@src/components/sections/BenefitsSection.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FAQSection accordion</name>
  <files>src/components/sections/FAQSection.tsx</files>
  <action>
Replace the placeholder stub entirely. This component MUST be "use client" — the accordion requires React state.

**Constraints (from PROJECT.md):** No new packages. Use only React state + Tailwind CSS transitions — no @headlessui/react, no framer-motion, no external accordion library.

**Color tokens:** `brand-text` (#282828), `brand-muted` (#5C5C5C), `font-mono` = Courier Prime.

**FAQ data** — hardcode at top of file:
```ts
const faqItems = [
  {
    q: "Who can register?",
    a: "Open to all developers, students, and AI enthusiasts in Malaysia. No prior AI experience is required — bring curiosity and a laptop.",
  },
  {
    q: "How large can teams be?",
    a: "Teams of 1 to 4 members. Solo participants are welcome and will have opportunities to find teammates during the opening session.",
  },
  {
    q: "What should I build?",
    a: "Any project that meaningfully uses Google AI technologies — Gemini API, Vertex AI, Firebase AI, or Gemma. The theme is AI for Real Impact.",
  },
  {
    q: "Are there prizes?",
    a: "Winning teams receive Google hardware bundles, Google Cloud credits, and featured placement across GDG KL community channels.",
  },
  {
    q: "Do I need to bring a laptop?",
    a: "Yes, bring your own device. WiFi and power will be provided. Google Cloud credits will be distributed to all registered teams before the hackathon starts.",
  },
  {
    q: "Is food provided?",
    a: "Meals and refreshments are included for all registered participants for both days of the event.",
  },
];
```

**State:** `const [openItems, setOpenItems] = useState<Set<number>>(new Set());`

**Toggle function** — adds index to set if absent, removes if present (supports multi-open, no auto-close):
```ts
const toggle = (i: number) => {
  setOpenItems(prev => {
    const next = new Set(prev);
    next.has(i) ? next.delete(i) : next.add(i);
    return next;
  });
};
```

**Header row pattern** — same as BenefitsSection:
```tsx
<div className="flex items-center gap-3 mb-4">
  <span className="font-mono text-xs font-bold border border-brand-text px-2 py-0.5 tracking-wider">
    [ FAQ ]
  </span>
  <span className="font-mono text-xs text-brand-muted tracking-widest">{`>>>`}</span>
  <div className="flex-1 border-t border-dashed border-brand-muted/60" />
</div>
```

**FAQ list container:** `<div className="border border-brand-text">`

**Each item structure:**
```tsx
<div key={i} className="border-b border-brand-muted/30 last:border-b-0">
  {/* Question row — clickable button */}
  <button
    onClick={() => toggle(i)}
    className="w-full flex justify-between items-center p-4 hover:bg-brand-text/5 text-left cursor-pointer"
  >
    <span className="font-mono text-sm font-bold text-brand-text">{item.q}</span>
    {/* Chevron SVG — rotate-180 when open */}
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      className={`w-4 h-4 shrink-0 ml-3 transition-transform duration-300 ${openItems.has(i) ? "rotate-180" : ""}`}
    >
      <polyline points="6 9 12 15 18 9" />
    </svg>
  </button>
  {/* Answer — smooth height animation via max-h transition */}
  <div
    className={`overflow-hidden transition-all duration-300 ease-in-out ${
      openItems.has(i) ? "max-h-48" : "max-h-0"
    }`}
  >
    <div className="px-4 pb-4 pt-1">
      <p className="font-mono text-sm text-brand-muted leading-relaxed">{item.a}</p>
    </div>
  </div>
</div>
```

**Section wrapper:** `<section className="px-6 lg:px-8 py-6">`

**Complete file structure:**
```tsx
"use client";

import { useState } from "react";

const faqItems = [...]; // as above

export function FAQSection() {
  const [openItems, setOpenItems] = useState<Set<number>>(new Set());

  const toggle = (i: number) => {
    setOpenItems(prev => {
      const next = new Set(prev);
      next.has(i) ? next.delete(i) : next.add(i);
      return next;
    });
  };

  return (
    <section className="px-6 lg:px-8 py-6">
      {/* header */}
      <div className="flex items-center gap-3 mb-4">
        <span className="font-mono text-xs font-bold border border-brand-text px-2 py-0.5 tracking-wider">
          [ FAQ ]
        </span>
        <span className="font-mono text-xs text-brand-muted tracking-widest">{`>>>`}</span>
        <div className="flex-1 border-t border-dashed border-brand-muted/60" />
      </div>
      {/* accordion list */}
      <div className="border border-brand-text">
        {faqItems.map((item, i) => (
          <div key={i} className="border-b border-brand-muted/30 last:border-b-0">
            <button
              onClick={() => toggle(i)}
              className="w-full flex justify-between items-center p-4 hover:bg-brand-text/5 text-left cursor-pointer"
            >
              <span className="font-mono text-sm font-bold text-brand-text">{item.q}</span>
              <svg
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                strokeWidth="2"
                className={`w-4 h-4 shrink-0 ml-3 transition-transform duration-300 ${openItems.has(i) ? "rotate-180" : ""}`}
              >
                <polyline points="6 9 12 15 18 9" />
              </svg>
            </button>
            <div
              className={`overflow-hidden transition-all duration-300 ease-in-out ${
                openItems.has(i) ? "max-h-48" : "max-h-0"
              }`}
            >
              <div className="px-4 pb-4 pt-1">
                <p className="font-mono text-sm text-brand-muted leading-relaxed">{item.a}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>
  );
}
```
  </action>
  <verify>Run `npx tsc --noEmit` from project root — exits 0 with no TypeScript errors. File begins with `"use client"`, exports `FAQSection`, uses `useState<Set<number>>`. Accordion markup has `max-h-0`/`max-h-48` conditional classes and `rotate-180` on the chevron.</verify>
  <done>FAQSection.tsx implemented with 6 FAQ items, multi-open accordion state (Set&lt;number&gt;), smooth max-h transition animation, and chevron rotation. No TypeScript errors.</done>
</task>

</tasks>

<verification>
After task complete:
1. `npx tsc --noEmit` exits 0 from project root
2. FAQSection.tsx starts with `"use client"`
3. `openItems` is typed as `Set<number>` — multi-open, not a single index
4. Answer divs use `max-h-0` (collapsed) / `max-h-48` (expanded) with `transition-all duration-300`
5. Chevron uses `rotate-180` conditional class based on `openItems.has(i)`
6. No external accordion library imported — pure React + Tailwind
</verification>

<success_criteria>
- FAQSection.tsx exports `FAQSection` as named export
- 6 FAQ items rendered as clickable accordion entries
- Multiple items can be open simultaneously (Set-based state)
- Smooth CSS height transition (max-h-0 → max-h-48) on expand/collapse
- Chevron SVG rotates 180° when item is open
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-sections/04-02-SUMMARY.md`
</output>
