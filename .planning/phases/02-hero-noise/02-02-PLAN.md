---
phase: 02-hero-noise
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
autonomous: false
requirements:
  - SECT-01

must_haves:
  truths:
    - '"Build With AI" heading renders in Instrument Serif at large display size'
    - '"KUALA LUMPUR" renders in Workbench retro display font'
    - '"[[ SIGNAL RECEIVED ]]" badge renders in Courier Prime with white text on #282828 pill background'
    - "Description paragraph text is present in Courier Prime"
    - '"REGISTER NOW" CTA button renders as an outlined (border-only) button with hover fill effect'
    - "Twin Towers, KL Tower, and signal icon illustrations are positioned flanking the center content"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Full hero section component replacing the placeholder"
      min_lines: 60
      exports: ["HeroSection"]
    - path: "public/images/twin-towers.svg"
      provides: "Twin Towers decorative illustration (user-supplied)"
    - path: "public/images/kl-tower.svg"
      provides: "KL Tower decorative illustration (user-supplied)"
    - path: "public/images/signal-icon.svg"
      provides: "Signal icon illustration (user-supplied)"
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "font-display utility"
      via: "Tailwind font-display class on heading (maps to --font-instrument-serif)"
      pattern: "font-display"
    - from: "src/components/sections/HeroSection.tsx"
      to: "font-retro utility"
      via: "Tailwind font-retro class on KUALA LUMPUR text (maps to --font-workbench)"
      pattern: "font-retro"
    - from: "src/components/sections/HeroSection.tsx"
      to: "public/images/*.svg"
      via: "next/image src props referencing /images/ paths"
      pattern: "/images/(twin-towers|kl-tower|signal-icon)\\.svg"
---

<objective>
Replace the HeroSection placeholder with the full Figma-faithful hero section: display heading, retro city subheading, badge, description, CTA button, and flanking decorative SVG illustrations.

Purpose: The hero is the first thing visitors see — it establishes the visual identity of the event landing page with brand typography and the Kuala Lumpur city illustrations.
Output: A complete HeroSection.tsx component implementing all hero content per CONTEXT.md decisions and Figma specs.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-noise/02-CONTEXT.md
@src/components/sections/HeroSection.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement full HeroSection component</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <action>
    Replace the placeholder entirely. The component must be a named export `export function HeroSection()`.

    **Layout structure:** Three-column flex row at the desktop viewport level:
    - Left column: Twin Towers SVG illustration (decorative, flanking)
    - Center column: All text content stacked vertically, centered
    - Right column: KL Tower SVG + Signal icon illustrations (decorative, flanking)

    Use `flex items-end justify-between` on the outer section element, with generous vertical padding (`py-20` or `py-24`). Add `w-full` and `overflow-hidden`. The center column uses `flex flex-col items-center text-center gap-6`.

    **Center content — exact order top to bottom:**

    1. **Signal received badge**: `<span>` with text `[[ SIGNAL RECEIVED ]]`, Courier Prime (`font-mono`), white text (`text-white`), dark background pill (`bg-brand-text px-4 py-1 rounded-full text-sm tracking-widest uppercase`). This badge sits above the main heading.

    2. **Main heading**: `<h1>` with text `Build With AI`, font class `font-display` (maps to Instrument Serif via the @theme token), very large size. Use `text-7xl lg:text-8xl` and `leading-none`. Color: `text-brand-text`.

    3. **City subheading**: `<p>` with text `KUALA LUMPUR`, font class `font-retro` (maps to Workbench via the @theme token), large tracking (`tracking-widest`). Size: `text-4xl lg:text-5xl`. Color: `text-brand-text`. The Workbench font has a retro LED/display aesthetic — no special treatment needed beyond the font class.

    4. **Tagline / description**: `<p>` with exact text: `Gain real-world experience with Google's latest AI tools & models and start building the future today.` Font: `font-mono` (Courier Prime). Size: `text-sm`. Color: `text-brand-muted`. Max width: `max-w-md`.

    5. **CTA button**: `<a href="#">` styled as an outlined button. Use `inline-block border border-brand-text text-brand-text font-mono text-sm tracking-widest uppercase px-8 py-3 hover:bg-brand-text hover:text-white transition-colors duration-200`. Text: `REGISTER NOW`. This is per CONTEXT.md: outlined button (border only, no fill), hover fills with #282828 background and text inverts to white. Single element with CSS border (not a two-rectangle structure — simpler).

    **Left column (Twin Towers):**
    ```tsx
    <div className="shrink-0 self-end">
      <Image src="/images/twin-towers.svg" alt="Petronas Twin Towers" width={200} height={320} />
    </div>
    ```
    `self-end` aligns the illustration to the bottom of the flex row. Adjust width/height to match the SVG's actual aspect ratio once the user supplies it — these are initial estimates.

    **Right column (KL Tower + Signal icon, stacked):**
    ```tsx
    <div className="flex flex-col items-center gap-4 shrink-0 self-end">
      <Image src="/images/signal-icon.svg" alt="" width={80} height={80} aria-hidden="true" />
      <Image src="/images/kl-tower.svg" alt="KL Tower" width={120} height={280} />
    </div>
    ```
    Signal icon sits above KL Tower per Figma composition. The signal icon is decorative so use `alt=""` with `aria-hidden="true"`.

    **Import**: Add `import Image from "next/image"` at the top of the file.

    Use only Tailwind utility classes — no inline styles. The component must pass TypeScript strict mode. No `any` types. No unused imports. Keep the named export pattern (`export function HeroSection`) to match the barrel export in index.ts.
  </action>
  <verify>Run `npm run build` — must pass with zero TypeScript errors. Run `npm run lint` — must pass with zero lint errors.</verify>
  <done>Build and lint pass. HeroSection.tsx exports `HeroSection` with full content: badge, Instrument Serif heading via font-display, Workbench subheading via font-retro, Courier Prime description via font-mono, outlined CTA button, and three next/image references for the decorative illustrations. Placeholder content is fully removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Place illustration SVGs and verify hero matches Figma</name>
  <action>
    Human action required before verification: export the three illustration SVGs from Figma and place them in public/images/.

    Steps:
    1. Export Twin Towers SVG from Figma → save as `public/images/twin-towers.svg`
    2. Export KL Tower SVG from Figma → save as `public/images/kl-tower.svg`
    3. Export Signal icon SVG from Figma (two overlapping shapes: orange + grey with dark outlines) → save as `public/images/signal-icon.svg` as a single combined file
    4. Create `public/images/` directory if it does not exist (same directory as noise.png from plan 01)
  </action>
  <verify>
    1. Confirm all three SVG files exist in `public/images/`
    2. Run `npm run dev` and open http://localhost:3000
    3. Verify the hero section at the top of the page shows:
       - "[[ SIGNAL RECEIVED ]]" badge — dark pill background, white Courier Prime text
       - "Build With AI" — large Instrument Serif typeface (serif, elegant)
       - "KUALA LUMPUR" — Workbench retro LED font (distinctive character forms)
       - Description paragraph in Courier Prime below the headings
       - "REGISTER NOW" outlined button (border only, no background fill at rest)
       - Hover the button — it should fill dark (#282828) background with white text
       - Twin Towers illustration on the left side of the page
       - KL Tower + Signal icon stacked on the right side
    4. Overall layout should roughly match the Figma hero composition
  </verify>
  <done>Hero section visually matches the Figma design: correct typography hierarchy, badge, CTA button, and flanking illustrations all present and correctly styled.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npm run lint` passes
- HeroSection.tsx contains: font-display (Instrument Serif heading), font-retro (Workbench subheading), font-mono (badge + description + button), bg-brand-text (badge background), border-brand-text (button border), hover:bg-brand-text (button hover fill)
- next/image used for all three SVG illustrations with proper alt attributes
- Human confirmed: hero section visually matches Figma design
</verification>

<success_criteria>
The hero section renders the full Figma design: Instrument Serif "Build With AI" heading, Workbench "KUALA LUMPUR" subheading, signal received badge, description text, outlined CTA button with hover effect, and flanking city illustrations. All typography uses the font utility classes established in Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-noise/02-02-SUMMARY.md`
</output>
