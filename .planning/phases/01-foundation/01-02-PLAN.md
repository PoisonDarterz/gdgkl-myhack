---
phase: 01-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/BenefitsSection.tsx
  - src/components/sections/WhatIsBAISection.tsx
  - src/components/sections/StatsSection.tsx
  - src/components/sections/TimelineSection.tsx
  - src/components/sections/PartnersSection.tsx
  - src/components/sections/FAQSection.tsx
  - src/components/sections/RegisterCTASection.tsx
  - src/components/sections/FooterSection.tsx
  - src/components/sections/index.ts
  - app/page.tsx
autonomous: true
requirements:
  - SETUP-02

must_haves:
  truths:
    - "http://localhost:3000 loads without errors — no import failures or missing module crashes"
    - "All 9 section placeholders are visible in correct Figma scroll order when scrolling the page"
    - "Each placeholder is identifiable by its section name (via text or border label)"
    - "page.tsx is a clean landing page root — no Next.js default boilerplate content remains"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section placeholder component"
      exports: ["HeroSection"]
    - path: "src/components/sections/BenefitsSection.tsx"
      provides: "Benefits section placeholder component"
      exports: ["BenefitsSection"]
    - path: "src/components/sections/WhatIsBAISection.tsx"
      provides: "What is BAI section placeholder component"
      exports: ["WhatIsBAISection"]
    - path: "src/components/sections/StatsSection.tsx"
      provides: "Stats section placeholder component"
      exports: ["StatsSection"]
    - path: "src/components/sections/TimelineSection.tsx"
      provides: "Timeline section placeholder component"
      exports: ["TimelineSection"]
    - path: "src/components/sections/PartnersSection.tsx"
      provides: "Partners section placeholder component"
      exports: ["PartnersSection"]
    - path: "src/components/sections/FAQSection.tsx"
      provides: "FAQ section placeholder component"
      exports: ["FAQSection"]
    - path: "src/components/sections/RegisterCTASection.tsx"
      provides: "Register CTA section placeholder component"
      exports: ["RegisterCTASection"]
    - path: "src/components/sections/FooterSection.tsx"
      provides: "Footer section placeholder component"
      exports: ["FooterSection"]
    - path: "src/components/sections/index.ts"
      provides: "Barrel export for all section components"
      contains: "export"
    - path: "app/page.tsx"
      provides: "Landing page root composing all sections"
      contains: "HeroSection"
  key_links:
    - from: "app/page.tsx"
      to: "src/components/sections/index.ts"
      via: "barrel import"
      pattern: "from.*@/components/sections"
    - from: "src/components/sections/index.ts"
      to: "individual section files"
      via: "named re-exports"
      pattern: "export.*Section"
---

<objective>
Create placeholder section components for all 9 sections of the landing page, export them through a barrel index, and replace the default page.tsx with a clean landing page root that renders all sections in Figma scroll order.

Purpose: Establishes the complete structural skeleton of the page so every subsequent phase can implement sections independently without changing import paths or scroll order.
Output: 9 section component files, barrel index, and landing page root in page.tsx.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder section components and barrel export</name>
  <files>
    src/components/sections/HeroSection.tsx
    src/components/sections/BenefitsSection.tsx
    src/components/sections/WhatIsBAISection.tsx
    src/components/sections/StatsSection.tsx
    src/components/sections/TimelineSection.tsx
    src/components/sections/PartnersSection.tsx
    src/components/sections/FAQSection.tsx
    src/components/sections/RegisterCTASection.tsx
    src/components/sections/FooterSection.tsx
    src/components/sections/index.ts
  </files>
  <action>
    Create the directory `src/components/sections/` and populate it with 9 section component files plus a barrel export.

    IMPORTANT: The project root is at `E:/Programming/Projects/myhack/`. The `src/` directory does NOT exist yet — create it. The path is `src/components/sections/` relative to the project root (i.e., `E:/Programming/Projects/myhack/src/components/sections/`).

    Each section component follows this exact pattern (substitute SectionName and section-name-label):
    ```tsx
    export function SectionName() {
      return (
        <section className="w-full border border-dashed border-brand-muted/30 py-16">
          {/* SectionName placeholder */}
          <p className="font-mono text-xs text-brand-muted text-center tracking-widest uppercase">
            [ section-name-label ]
          </p>
        </section>
      );
    }
    ```

    Create these 9 files with these component names and labels:
    1. HeroSection.tsx — component: HeroSection — label: "Hero"
    2. BenefitsSection.tsx — component: BenefitsSection — label: "Benefits"
    3. WhatIsBAISection.tsx — component: WhatIsBAISection — label: "What is Build With AI?"
    4. StatsSection.tsx — component: StatsSection — label: "Stats"
    5. TimelineSection.tsx — component: TimelineSection — label: "Timeline"
    6. PartnersSection.tsx — component: PartnersSection — label: "Partners"
    7. FAQSection.tsx — component: FAQSection — label: "FAQ"
    8. RegisterCTASection.tsx — component: RegisterCTASection — label: "Register CTA"
    9. FooterSection.tsx — component: FooterSection — label: "Footer"

    Then create `src/components/sections/index.ts` with named re-exports for all 9:
    ```ts
    export { HeroSection } from "./HeroSection";
    export { BenefitsSection } from "./BenefitsSection";
    export { WhatIsBAISection } from "./WhatIsBAISection";
    export { StatsSection } from "./StatsSection";
    export { TimelineSection } from "./TimelineSection";
    export { PartnersSection } from "./PartnersSection";
    export { FAQSection } from "./FAQSection";
    export { RegisterCTASection } from "./RegisterCTASection";
    export { FooterSection } from "./FooterSection";
    ```

    Note on Tailwind class `text-brand-muted`: This maps to `--color-brand-muted: #5C5C5C` registered in globals.css by Plan 01. Use it as-is — Tailwind v4 resolves `--color-brand-muted` to the `text-brand-muted` utility automatically.
    Note on `border-brand-muted/30`: opacity modifier on color token — valid in Tailwind v4.
  </action>
  <verify>
    Run `ls E:/Programming/Projects/myhack/src/components/sections/` — must show 10 files (9 section tsx + index.ts).
    Run `cd E:/Programming/Projects/myhack && npx tsc --noEmit 2>&1 | head -20` — must show no import or export errors for the section files.
  </verify>
  <done>
    All 9 section component files exist in src/components/sections/, each exports a named React component. index.ts re-exports all 9. TypeScript compiles without errors on these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace page.tsx with landing page root rendering all sections in order</name>
  <files>app/page.tsx</files>
  <action>
    Replace the entire content of app/page.tsx with a clean landing page root. Remove all existing boilerplate (Image imports, Next.js template links, etc.).

    The new page.tsx:
    ```tsx
    import {
      HeroSection,
      BenefitsSection,
      WhatIsBAISection,
      StatsSection,
      TimelineSection,
      PartnersSection,
      FAQSection,
      RegisterCTASection,
      FooterSection,
    } from "@/components/sections";

    export default function Home() {
      return (
        <main className="w-full bg-brand-bg text-brand-text">
          <HeroSection />
          <BenefitsSection />
          <WhatIsBAISection />
          <StatsSection />
          <TimelineSection />
          <PartnersSection />
          <FAQSection />
          <RegisterCTASection />
          <FooterSection />
        </main>
      );
    }
    ```

    The scroll order (top to bottom) is exactly: Hero → Benefits → What is BAI → Stats → Timeline → Partners → FAQ → Register CTA → Footer. This is the Figma scroll order and must not be changed.

    `bg-brand-bg` uses the `--color-brand-bg: #F5F5F5` token from globals.css.
    `text-brand-text` uses the `--color-brand-text: #282828` token from globals.css.

    The `@/components/sections` path alias resolves to `src/components/sections/index.ts` via the tsconfig.json paths — verify tsconfig has `"@/*": ["./src/*"]` or `"@/*": ["./*"]`. If the alias is `"@/*": ["./*"]` (pointing to project root), the import path would be `@/src/components/sections`. Read tsconfig.json to confirm before writing the import.
  </action>
  <verify>
    Run `cd E:/Programming/Projects/myhack && npm run build 2>&1 | tail -20` — must complete without errors.
    Run `cd E:/Programming/Projects/myhack && npm run dev &` then check `curl -s http://localhost:3000 | grep -i "hero\|benefits\|stats"` — must find placeholder labels in the HTML.
    Kill the dev server after checking: `pkill -f "next dev"` or `kill %1`.
  </verify>
  <done>
    page.tsx imports all 9 sections from barrel, renders them in Figma scroll order, build passes, and the running dev server serves a page containing all 9 section placeholders in correct order with #F5F5F5 background.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` passes without TypeScript or module errors
2. `npm run dev` serves the page and all 9 placeholder sections are visible in the browser when scrolling
3. Page background is #F5F5F5 (brand off-white)
4. Each section shows its label text so the scaffold is traceable (e.g., "[ Hero ]", "[ Benefits ]")
5. No Next.js boilerplate content (logos, template links) remains visible
6. Section order from top to bottom matches Figma: Hero, Benefits, What is BAI, Stats, Timeline, Partners, FAQ, Register CTA, Footer
</verification>

<success_criteria>
- All 9 section files exist in src/components/sections/
- Barrel index exports all 9 section components
- page.tsx renders all 9 sections in Figma scroll order
- No default Next.js boilerplate content in page.tsx
- Build and TypeScript compilation pass without errors
- Dev server renders the page with correct #F5F5F5 background and all section placeholders visible
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
