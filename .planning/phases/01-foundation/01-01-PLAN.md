---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/globals.css
autonomous: true
requirements:
  - SETUP-01
  - STYLE-01

must_haves:
  truths:
    - "Browser renders page with background color #F5F5F5 instead of default white"
    - "Courier Prime font is loaded and applied as the default monospace body font"
    - "Instrument Serif and Workbench fonts are loaded with CSS variables available"
    - "Page metadata reads Build With AI KL 2026 in browser tab"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Font loading via next/font/google and layout wrapper with CSS variable classes"
      contains: "CourierPrime, InstrumentSerif, Workbench"
    - path: "app/globals.css"
      provides: "Tailwind v4 theme tokens for font families and brand colors"
      contains: "--font-display"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS variable classes applied to wrapper div"
      pattern: "font-display|font-retro|font-mono"
    - from: "app/globals.css"
      to: "Tailwind utility classes"
      via: "@theme inline block font and color tokens"
      pattern: "--color-brand-bg"
---

<objective>
Load Courier Prime, Instrument Serif, and Workbench fonts via next/font/google, apply them through a wrapper div in layout.tsx, and register Tailwind v4 CSS theme tokens for the three font families and the brand color palette.

Purpose: Every subsequent phase builds on these font classes and color tokens. Getting them right now prevents retroactive search-and-replace across the whole page.
Output: Updated layout.tsx with font loading and wrapper div, updated globals.css with font and color tokens.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@app/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load three custom fonts in layout.tsx and apply via wrapper div</name>
  <files>app/layout.tsx</files>
  <action>
    Replace the existing layout.tsx with updated font loading and a wrapper div approach.

    Font loading (next/font/google):
    - Import CourierPrime with subsets: ["latin"], variable: "--font-courier-prime"
    - Import InstrumentSerif with subsets: ["latin"], variable: "--font-instrument-serif"
    - Import Workbench with subsets: ["latin"], variable: "--font-workbench"
    - Keep existing Geist and Geist_Mono imports with their variables (they are fallbacks — do NOT remove them)

    Layout structure change:
    - The `<body>` keeps the existing Geist variable classes for fallback: `${geistSans.variable} ${geistMono.variable} antialiased`
    - Add a new `<div>` as the first child of `<body>` that wraps `{children}`:
      ```tsx
      <div className={`${courierPrime.variable} ${instrumentSerif.variable} ${workbench.variable} font-mono min-h-screen`}>
        {children}
      </div>
      ```
    - The wrapper div applies all three custom font variables AND sets `font-mono` as the default text class (Courier Prime inherits for all text inside)
    - Instrument Serif and Workbench are available as CSS variables but only activated by `font-display` and `font-retro` classes respectively (defined in globals.css tokens)

    Metadata update (in the same file):
    - Change title to: "Build With AI KL 2026"
    - Change description to: "Build With AI Kuala Lumpur — 2026 developer community event"

    Note: Do NOT apply font classes on `<html>` — only on the wrapper div inside `<body>`.
  </action>
  <verify>
    Run `cd E:/Programming/Projects/myhack && npm run build 2>&1 | tail -20` — must complete without TypeScript or module errors related to font imports.
    Check that the three font variable names appear in the compiled output or confirm no build errors.
  </verify>
  <done>
    layout.tsx imports all five fonts (Geist, Geist_Mono, CourierPrime, InstrumentSerif, Workbench), metadata reflects Build With AI KL 2026, wrapper div applies all three custom font variables plus font-mono class, and `npm run build` passes without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register font and color tokens in globals.css via Tailwind v4 @theme</name>
  <files>app/globals.css</files>
  <action>
    This project uses Tailwind v4, which uses CSS-based configuration — there is NO tailwind.config.ts file. All custom tokens are registered in the existing `@theme inline {}` block in globals.css.

    Add to the existing `@theme inline {}` block (do not replace existing entries, only append):

    Font family tokens — these map CSS variables (set by next/font on the wrapper div) to Tailwind utility classes:
    ```css
    --font-mono: var(--font-courier-prime), ui-monospace, monospace;
    --font-display: var(--font-instrument-serif), Georgia, serif;
    --font-retro: var(--font-workbench), monospace;
    ```
    Note: `--font-mono` will override the existing `--font-mono: var(--font-geist-mono)` entry in the block — this is intentional, Courier Prime is now the primary monospace font.

    Color tokens — add these brand colors as new entries:
    ```css
    --color-brand-bg: #F5F5F5;
    --color-brand-text: #282828;
    --color-brand-muted: #5C5C5C;
    ```

    Background color — update the `:root` CSS block to change `--background` to the brand background:
    ```css
    --background: #F5F5F5;
    ```
    This makes `bg-background` render as #F5F5F5 instead of white. The existing Tailwind shadcn tokens remain unchanged; only `--background` value changes.

    Do NOT remove any existing CSS. Do NOT touch the `.dark {}` block variables.
  </action>
  <verify>
    Run `cd E:/Programming/Projects/myhack && npm run build 2>&1 | tail -10` — must complete without CSS parse errors.
    Verify globals.css contains `--color-brand-bg: #F5F5F5` and `--font-display: var(--font-instrument-serif)` by reading the file.
  </verify>
  <done>
    globals.css @theme block contains --font-mono (Courier Prime), --font-display (Instrument Serif), --font-retro (Workbench), and the three brand color tokens. The --background value in :root is #F5F5F5. Build passes without errors.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` completes without errors
2. `npm run dev` starts and http://localhost:3000 renders with #F5F5F5 background (not white)
3. Browser DevTools computed styles on the wrapper div show font-family starting with Courier Prime
4. Browser tab shows "Build With AI KL 2026"
</verification>

<success_criteria>
- layout.tsx loads CourierPrime, InstrumentSerif, and Workbench fonts via next/font/google
- Wrapper div inside body applies all three font CSS variables and font-mono class
- Geist/Geist_Mono remain as fallbacks on body
- globals.css registers font-display and font-retro Tailwind utilities
- Background color is #F5F5F5 (brand off-white) system-wide
- Brand text colors (#282828, #5C5C5C) are registered as Tailwind tokens
- Page metadata updated to Build With AI KL 2026
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
