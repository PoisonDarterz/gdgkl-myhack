---
phase: 03-mid-sections
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified: []
autonomous: false
requirements:
  - SECT-02
  - SECT-03
  - SECT-04

must_haves:
  truths:
    - "All three mid sections (Benefits, WhatIsBAI, Stats) are visually verified against the Figma design"
    - "No placeholder content remains in any of the three sections"
    - "Animations (typewriter and count-up) work correctly in the browser"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint — confirm all three mid sections match the Figma design in a real browser before phase is marked complete.

Purpose: Catch visual discrepancies between implementation and Figma that automated checks cannot detect (spacing, typography, proportions, animation feel).
Output: Human confirmation that Benefits, WhatIsBAI, and Stats sections are correct.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-mid-sections/03-01-SUMMARY.md
@.planning/phases/03-mid-sections/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final lint and build checks</name>
  <files></files>
  <action>
Run both checks to confirm the codebase is clean after all three section implementations:

```bash
npm run lint
npm run build
```

If lint errors exist, fix them before proceeding. If build fails, diagnose and fix TypeScript/import errors. Only proceed to the checkpoint when both pass.
</action>
<verify>
`npm run lint` exits with code 0 (no errors).
`npm run build` exits with code 0 (no TypeScript errors, build succeeds).
</verify>
<done>Both `npm run lint` and `npm run build` pass with exit code 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — all three mid sections match Figma</name>
  <action>Human reviews the running dev server to visually confirm Benefits, WhatIsBAI, and Stats sections match the Figma design. Animations (typewriter and count-up) are tested by scrolling.</action>
  <what-built>
Three mid sections fully implemented:
1. BenefitsSection — three vertically stacked cards with '#' prefix headings, dot/dashed separator lines, hover effects
2. WhatIsBAISection — terminal block with off-white background, window chrome (traffic light dots + title bar), typewriter animation on scroll
3. StatsSection — 2,258 events and 178,000 developers trained figures with count-up animation on scroll
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000 in your browser
2. Scroll past the Hero section to reach the Benefits section
   - Confirm: three cards are visible, each with a '#' prefix heading
   - Confirm: separator lines (dots or dashes) appear between each card
   - Confirm: hovering over a card shows a subtle visual change
3. Continue scrolling to the "What is Build With AI?" section
   - Confirm: terminal block visible with off-white background (NOT dark) and a border
   - Confirm: window chrome shows three colored dots (red/yellow/green) and a title
   - Confirm: text types out character-by-character when section enters the viewport
   - Confirm: re-scrolling up and back down does NOT retrigger the animation
4. Continue scrolling to the Stats section
   - Confirm: two stat figures are visible (2,258 and 178,000 with supporting labels)
   - Confirm: numbers count up from 0 when the section enters the viewport
   - Confirm: re-scrolling up and back down does NOT re-trigger count-up
5. Cross-check the Figma design for all three sections — note any layout, spacing, or typography issues
  </how-to-verify>
  <resume-signal>Type "approved" if all three sections match the Figma design, or describe specific issues to fix (e.g., "card spacing too wide", "stat label copy wrong", "terminal chrome color off")</resume-signal>
</task>

</tasks>

<verification>
All checks automated in Task 1. Human visual verification performed in Task 2 checkpoint.
</verification>

<success_criteria>

- `npm run lint` passes
- `npm run build` passes
- Human confirms Benefits, WhatIsBAI, and Stats sections visually match Figma
- All animations (typewriter and count-up) work correctly and trigger only once on first scroll-into-view
  </success_criteria>

<output>
After completion, create `.planning/phases/03-mid-sections/03-03-SUMMARY.md`
</output>
