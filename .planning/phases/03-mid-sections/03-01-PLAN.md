---
phase: 03-mid-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/BenefitsSection.tsx
  - src/components/sections/StatsSection.tsx
autonomous: true
requirements:
  - SECT-02
  - SECT-04

must_haves:
  truths:
    - "Benefits section shows three vertically stacked feature cards with '#' prefix headings"
    - "Each Benefits card has a dot separator line between cards"
    - "Benefits cards respond to hover with a subtle background or shadow change"
    - "Stats section displays '2,258' and '178,000' figures with supporting labels"
    - "Stats numbers animate from 0 to their final value when the section scrolls into view"
  artifacts:
    - path: "src/components/sections/BenefitsSection.tsx"
      provides: "Three feature cards with '#' prefix headings, dot separators, hover effects"
      min_lines: 40
    - path: "src/components/sections/StatsSection.tsx"
      provides: "Count-up stats with IntersectionObserver scroll trigger"
      min_lines: 50
  key_links:
    - from: "src/components/sections/StatsSection.tsx"
      to: "IntersectionObserver callback"
      via: "useEffect + useRef"
      pattern: "IntersectionObserver"
    - from: "StatsSection count-up interval"
      to: "final value (2258 / 178000)"
      via: "setInterval incrementing toward target"
      pattern: "setInterval|requestAnimationFrame"
---

<objective>
Implement the Benefits and Stats sections, replacing their placeholder components with complete, pixel-faithful layouts matching the Figma design.

Purpose: These two sections present the core value proposition (Benefits cards) and social proof (Stats figures) of the Build With AI KL event — critical content visible as users scroll past the hero.
Output: BenefitsSection with three interactive cards; StatsSection with animated count-up figures triggered on scroll.
</objective>

<execution_context>
@C:/Users/youknowwhoiam/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/youknowwhoiam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mid-sections/03-CONTEXT.md
@src/components/sections/HeroSection.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BenefitsSection with three cards, dot separators, and hover effects</name>
  <files>src/components/sections/BenefitsSection.tsx</files>
  <action>
Replace the placeholder BenefitsSection with a complete implementation:

**Layout:**

- Section uses the same off-white (#F5F5F5) background — no separate background color needed (inherits `bg-background`)
- Cards are stacked vertically (flex-col), full-width or max-w-3xl/4xl centered, with consistent padding
- Section has vertical padding matching Figma (use py-16 or py-20 as baseline — adjust to match Figma spacing visually)

**Each card structure:**

- Border: `border border-brand-text` (same dark border as hero card) — check figma_node.json for exact border treatment
- Heading uses `#` prefix in `font-mono text-brand-text` — heading itself in a larger font-mono or font-display as per Figma
- Body copy below heading in `font-mono text-brand-muted text-sm` or matching Figma size
- Card padding: `p-6` or `p-8` depending on Figma

**Dot separator lines between cards:**

- Between card 1 and 2, and between card 2 and 3: a horizontal rule made of dots
- Use a `<div>` with `font-mono text-brand-muted tracking-widest text-center overflow-hidden` containing repeated `.` characters, or a CSS `border-dotted` approach
- Pattern: `..........................................................` spanning full width, or use `border-t border-dashed border-brand-muted/40` if that matches Figma

**Hover state** (locked decision: subtle hover on each card):

- Since hover is purely CSS, no "use client" needed
- Add `hover:bg-brand-text/5` or `hover:shadow-sm` with `transition-colors duration-200` on the card container
- Use `group` on card if any child needs hover-driven changes

**Card content** — extract exact copy from figma_node.json by searching for nodes named "Benefits" or near the BenefitsSection area. If Figma content is not accessible, use placeholder:

- Card 1: `# 01 — Learn by Building` + "Hands-on workshops using Google's latest AI tools and models."
- Card 2: `# 02 — Real Skills` + "Take home practical techniques you can apply to your own projects."
- Card 3: `# 03 — Community` + "Connect with developers building with AI across Kuala Lumpur."

This component does NOT need "use client" — pure CSS hover is sufficient.

Verify: `npm run lint` passes with no errors.
</action>
<verify>
Run `npm run lint` — expect 0 errors.
Open dev server (`npm run dev`) and scroll to the Benefits section: three cards visible, stacked vertically, each with '#' prefix heading, dot/dashed separator lines between cards, hover produces subtle visual change.
</verify>
<done>Three benefit cards are stacked vertically with '#' prefix headings, separator lines between them, and a hover effect. `npm run lint` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement StatsSection with count-up animation on scroll-into-view</name>
  <files>src/components/sections/StatsSection.tsx</files>
  <action>
Replace the placeholder StatsSection with a fully animated implementation. This component REQUIRES "use client" at the top.

**Layout:**

- Section uses the same off-white (#F5F5F5) background (inherits from page)
- Two stat figures stacked vertically (flex-col gap-12 or similar), centered or left-aligned per Figma
- Section vertical padding matching Figma (py-16 or py-20)

**Each stat block:**

- Large number displayed in `font-display text-6xl lg:text-8xl text-brand-text` (or match Figma typography exactly — check figma_node.json for font sizing)
- Supporting label in `font-mono text-brand-muted text-sm tracking-widest uppercase` below the number
- Stats values: `2,258` (label: "events" or "events held globally" — extract exact label from Figma) and `178,000` (label: "developers trained" or similar — extract exact label from Figma)

**Count-up animation implementation:**

- Use `useRef` + `IntersectionObserver` to detect when section enters viewport
- On intersection (once), start count-up for both numbers simultaneously
- Count-up logic: `setInterval` incrementing a display value from 0 to final target over ~1.5s duration
  - For 2258: increment by ~15-20 per frame at 30fps (50ms interval)
  - For 178000: increment by ~1200 per frame at 30fps (50ms interval)
  - Use easing: linear is fine, or ease-out by reducing increment as target approached
  - Clear interval when display value >= target, snap to exact final value
- Use `useState` for both display values (initialized to 0 for clean animation start; SSR renders 0 which is acceptable for a below-fold section)
- Use `useRef` for the section element and IntersectionObserver instance
- Disconnect observer after first trigger to avoid re-running

**State pattern** (follow HeroSection pattern for setInterval cleanup):

```tsx
"use client";
import { useEffect, useRef, useState } from "react";

export function StatsSection() {
  const [events, setEvents] = useState(0);
  const [devs, setDevs] = useState(0);
  const sectionRef = useRef<HTMLElement>(null);

  useEffect(() => {
    const el = sectionRef.current;
    if (!el) return;

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          observer.disconnect();
          // start count-up intervals here
        }
      },
      { threshold: 0.3 }
    );
    observer.observe(el);
    return () => observer.disconnect();
  }, []);

  return (
    <section ref={sectionRef} ...>
      ...
    </section>
  );
}
```

Format display values with commas: `events.toLocaleString()` and `devs.toLocaleString()`.
</action>
<verify>
Run `npm run lint` — expect 0 errors.
Open dev server (`npm run dev`). Scroll down to the Stats section: numbers start at 0 and count up to 2,258 and 178,000 (formatted with commas) when the section enters the viewport. Numbers do not re-animate on subsequent scrolls.
</verify>
<done>StatsSection displays 2,258 events and 178,000 developers trained figures with count-up animation triggered once on scroll-into-view. `npm run lint` passes.</done>
</task>

</tasks>

<verification>
1. `npm run lint` — 0 errors across both files
2. `npm run build` — production build succeeds (no TypeScript errors)
3. Dev server: scroll to Benefits — three cards visible with '#' headings, separators, hover effects
4. Dev server: scroll to Stats — count-up animation fires once on scroll-into-view with 2,258 and 178,000 as final values
</verification>

<success_criteria>

- BenefitsSection: three vertically stacked cards, '#' prefix headings, dot/dashed separators between cards, hover effect applied
- StatsSection: two stat figures, count-up from 0 to final value on first scroll-into-view, numbers formatted with commas
- Both components are lint/TypeScript clean
- `npm run build` succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/03-mid-sections/03-01-SUMMARY.md`
</output>
