---
quick_id: "260421"
slug: dsh-judge-retry
description: Dashboard judge — show Retry button on failed rows
date: 2026-04-21
---

# Quick Task 260421: Dashboard judge retry button

## Task

In `app/dashboard/judge/page.tsx`, when a team row has `status === 'error'`, show a distinct "Retry" button instead of the generic "Run" button.

## Plan

### Task 1: Split Run/Retry buttons in the Actions column

**File:** `app/dashboard/judge/page.tsx` (line ~753)

**Action:** Split the combined `(pending || error)` condition into two separate buttons:
- `pending` → "Run" button (black, existing style)
- `error` → "Retry" button (red-600, communicates failure context)

**Done:** Error rows show a red "Retry" button; pending rows show the original black "Run" button.
